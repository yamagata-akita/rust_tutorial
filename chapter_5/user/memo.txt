スタック領域とヒープ領域

スタック領域
確保と開放→速い
領域の大きさ→小さい

ヒープ領域
確保と開放→遅い
領域の大きさ→大きい

スタック領域にはコンパイル時に大きさが決まっているデータが置かれる
データの大きさが変わるものはヒープ領域に入る

囲碁のデータを全部配列にして大きさを固定したら速度アップする？


v.push()
v.pop()
v.insert()
v.remove()

v.append()
v.extend_from_slice()